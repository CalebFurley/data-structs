#include <iostream>
using namespace std;

class ArrayClass {
  size_t _size;
  int* _array;
};

int main() {
  //make array of arrays, then access via array[row][col]

  //also good idea to start counting primitive operations (for algs)

  //return & for a[i] = 10 overloading

  //write an abstract array class (allow for matrices as well?)

  //_size = i
  //if(array!=null) delete array
  //for(int : array) make array

  //create a base level exception STRUCT, all exceptions inherit from

  //arrayclass<int>(arrayclass<int>*); <- check this out

  //video lectures on BIGO <------------- !!!!!

  //bucket sorting

  return 0;
}