#include <iostream>

int main(int argc, char** argv)
{
  // look into and write a deep copy. (not bit by bit, but actually copying over the data.)

  // if you create pointers inside an object, MUST MUST MUST, delete them in destrucor or memory leak.

  std::cout << "Shalom, Universe?" << std::endl;
  return 0; 
}